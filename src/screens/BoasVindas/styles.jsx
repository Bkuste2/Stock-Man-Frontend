import { StyleSheet } from "react-native";

export const container = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:"#000",
        alignItems:"center",
        justifyContent:"center",
    }
})